import { PREFIX_NAME } from "../../const";

export const BUTTON_CARD_NAME = `${PREFIX_NAME}-button-card`;

export const BUTTON_CARD_STATE_COLOR = {
    default: "var(--rgb-multitool-default)",
    positive: "var(--rgb-multitool-positive)",
    negative: "var(--rgb-multitool-negative)",
    notice: "var(--rgb-multitool-notice)", 
    error: "var(--rgb-multitool-error)",
};
